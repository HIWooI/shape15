# TODO

정확도 수치는 전부 **동일 홀드아웃**(마지막 2클립 `taeguk_1st` + `video.mov`, 4,697프레임)
기준이고, 학습은 나머지 13클립 4,699프레임이다. 배경·근거는 `PLAN.md`.

**목표 로봇은 K1이다.** G1은 교사가 ~100 fps로 빨라서 아이디어를 싸게 검증하는 용도이고,
검증이 끝나면 K1으로 옮겨 K1 숫자를 결과로 보고한다.

---

## 0. 진행 중 (자동)

- [ ] **K1 재라벨링** — 업그레이드된 soma-retargeter(PR#3)로 다시 생성
  - 실데이터 14/15 클립 → 합성 0/24 (총 ~40분)
  - 끝나면 `data/`로 복사할 것. tmp는 잡 삭제와 함께 사라진다
  ```
  cp $TMP/big4_k1.npz $TMP/synth_k1.npz data/
  ```

## 1. K1 기준선 재측정 — 최우선

이전 K1 수치(IK 28.70°, MLP 15.33°, res4 13.97°)는 **왼다리가 50% 프레임에서 망가진
라벨** 기준이라 전부 무효다. 새 라벨로 처음부터 다시 잰다.

- [ ] **IK 기준선 + MLP** — `distill.py`가 둘 다 한 번에 낸다
  ```
  .venv-ik/bin/python distill.py data/big4_k1.npz --features pose --width 1024 \
      --model models/k1_retarget_mlp.pt
  ```
- [ ] **오프셋 재생성** (지금 `fixtures/k1_joint_offsets.npz`가 없어 워커는 오프셋 0으로 동작)
  ```
  .venv-ik/bin/python make_offsets.py data/big4_k1.npz
  ```
- [ ] **병리 프레임 비율 재확인** — 구 라벨 50.4% → 새 라벨 1.0%(clip 17 기준)가
  전체 데이터셋에서도 유지되는지
- [ ] **렌더로 눈 검증** — 숫자만 믿지 말 것. 이번 사태의 교훈
  ```
  # tmp/render_dataset.py 를 새 라벨로 재실행
  ```

## 2. K1 정확도 올리기 (1번 완료 후, 각각 독립)

G1에서 검증된 개선 두 가지를 K1 새 라벨에 적용해 재확인한다.

- [ ] **residual + LayerNorm 구조** — G1/구K1에서 plain 2층 대비 −1.28° (시드 3회, ±0.17)
  - 4블록(8층)이 최적. 6·8블록은 다시 나빠짐. plain으로 깊게/넓게는 전부 악화
  - `distill.py`에 `--arch res` 옵션으로 넣을 것 (현재는 실험 스크립트에만 있음)
- [ ] **합성 데이터 추가** — G1에서 8.56° → 7.93° (−0.63°, 시드 노이즈 ±0.18 대비 유의)
  - 합성만으로 학습하면 18.68°(IK 수준). **보강재이지 대체재가 아니다**
- [ ] **병리/한계 영역 집중 합성** — 새 라벨에서도 특정 영역 오차가 크면.
  `make_synth.py`가 지금은 관절 범위 균일 샘플링이라 한계 근처가 희박하다

## 3. 라이브 배선 (K1 모델이 나온 뒤)

- [ ] **K1 MLP 라이브 확인** — `--mlp`는 체크포인트에서 로봇/DOF를 읽으므로 코드 변경 불필요
  ```
  GEM-X/.venv/bin/python demo_webcam.py --flip --robot k1 --stream 8080 \
      --no_imgfeat --mlp models/k1_retarget_mlp.pt
  ```
- [ ] **화면 멈춤 재확인** — 사용자가 반복해서 지적한 항목. 처리량이 아니라
  **프레임 간격**(`replay_delay.py`가 median/p99/max 출력)으로 판정하고, 반드시 눈으로 볼 것
  - 알려진 원인 순서: SAM-3D-Body 토큰 버스트(~280 ms, `--no_imgfeat`로 제거) →
    Isaac 경합 → 추적 실패 재검출

## 4. 열린 결함

- [ ] **G1 재캘리브레이션 미회복** — 재캘리브레이션 후 K1은 0.1 mm 복귀, G1은 158 mm.
  타겟은 정확(1.8e-7 m)하나 솔버가 더 나쁜 최소점에 남는다. `test_calibrate.py g1`에
  바운드로 기록됨. K1이 목표 로봇이므로 우선순위 낮음
- [ ] **왼무릎 비대칭** — rest pose에서 22.3° vs 0.0°. **업그레이드로 사라졌을 가능성이
  높다** — `test_apose.py`를 새 URDF/MJCF로 다시 돌려 확인할 것
- [ ] **다중 인물** — 추적이 bbox 전파뿐이라 여러 명이 있으면 엉뚱한 사람을 잡는다.
  onnxruntime은 고쳤으니 YOLOX+ByteTrack(62 ms/frame)이 가능

## 5. 속도 (15–30 Hz 목표)

현재 MLP 경로 ~17 Hz(loop 57 ms). 프로파일: VitPose 24.5 / GEM predict 16.5 / FK 6.2 /
MLP 1.2 ms. 워커(병렬 ~5 ms)는 루프를 막지 않는다.

- [ ] **kp2d 학생** — denoiser+FK를 잘라 ~30 Hz. G1에서 15.56°(MLP 11.48° 대비 −4.1°).
  **K1은 MLP 자체가 더 어려우므로 손실이 더 클 것** — 사용자는 MLP 경로를 선호했다.
  30 Hz가 실제로 필요해질 때만
- [ ] ~~FK 스킵(보조 t14 출력)~~ — **기각됨.** t14 예측 오차 179.7 mm로,
  "학습셋 평균 포즈 고정"(223.7 mm)보다 겨우 나은 수준. identity·scale·global_orient를
  다 줘도 안 움직임
- [ ] ~~지연 예측기~~ — **기각됨.** 132 ms 늦음의 비용이 +0.28°인데 예측기는 1.6–2.5°를
  지불한다. 기본 오차가 수 배 줄면 재검토

## 6. 위생

- [ ] **G1 라벨 재생성 검토** — G1 설정은 안 바뀌었지만 `newton_pipeline.py`가 1,141줄
  바뀌었다. G1 수치를 계속 인용하려면 재라벨링이 맞다(교사가 ~100 fps라 20분)
- [ ] **`replay_delay.py` / `record_input.py`의 mp4v** — VS Code(Chromium)는 H.264만
  재생한다. avc1은 이 OpenCV 빌드에 없으므로 mp4v로 쓰고 ffmpeg으로 변환하는 방식으로
- [ ] **`distill.py --test_clips` 기본값 2 고정 완료** — 예전 `len//4` 규칙이 클립 수가
  늘 때 조용히 스플릿을 바꿔 비교를 망쳤다. 다른 스크립트에도 같은 함정이 없는지

---

## 하지 말 것 (측정으로 기각된 것들)

- 학습 더 돌리기 — epoch 12에서 test 최저, 600까지 가면 train 0.12°로 암기만 는다
- 폭/깊이 무작정 키우기 — plain 구조는 w4096·6층에서 오히려 악화
- 시간 컨텍스트(과거 프레임 쌓기) — 15.4 → 15.2°, 노이즈 수준
- soma-retargeter 후처리 강제 ON — 병리 1.0% → 44.4%로 악화. 원작자 설정이 맞다
